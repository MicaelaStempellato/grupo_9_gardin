<!DOCTYPE html>
<html lang="en">

    <head>
        <!-- HEAD --->
        <%- include('../partials/head') %>
    </head>
        
    <body>
            
        <%- include('../partials/navbar') %>

        <div class="contenedor">

            <!-- HEADER --->
                

            <main>
                <article class="portada">
                    <!-- titulo foto y descripcion en detalle_curso -->

                    
                    <h1><%= product.nombre %></h1>
                    
                    <img src="/images/cursos/<%= product.image %>"   alt="Foto del curso" class="curso-img">
                    
                    <p class="descripcion_completa">
                        <%= product.descripcion %>
                    </p>
                   
                </article>

                <article class="programa">

                    <h2 class="desc_unidades">¿Que vamos a ver en el curso?</h2>
                    
                    <div class="unidades">

                        <% for ( let i = 0; i < product.unidades.length; i++ ) {  %>
                        
                            <p class="unidad"> <%= ("U"+(i+1)) + " - " + product.unidades[i] %> </p>

                        <% } %>
                    </div>

                
                    <h2 class="desc_requisitos">Herramientas necesarias</h2>
                    
                    <div class="requisitos">
                        <% for ( let i = 0; i < product.requisitos.length; i++ ) {  %>
                    
                            <p class="requisito"> <%= product.requisitos[i] %> </p>

                        <% } %>
                    </div>
                </article>


            </main>  

            <aside>
                <class class="botones">
                    <button class="buy_boton"><i class="fas fa-shopping-cart carrito"></i></button>
                    

                    <class class="modify_botones">
                        <form action="/products/delete/<%= product.id %>?_method=DELETE" method="POST">
                            <button type="submit" class="delete_boton"><i class="fas fa-trash-alt basura"></i></button>
                        </form>

                        <form action="/products/edit/<%= product.id %>?_method=PUT" method="POST">
                            <button type="submit" class="edit_boton"><i class="far fa-edit"></i></button>
                        </form>
                    </class>
                    
                    
                    
                </class>
                <div class="datos_curso">
                    
                    
                    <p class="dat__ precio">Precio: <%= "$" + product.precio %></p>    
                    <p class="dat__">Dictado por:  <%= product.profesor %> </p>
                    <p class="dat__">Dirigido a: <%= product.experiencia %> </p>
                    <p class="dat__">Ambiente: <%= product.ambiente %></p>
                    <p class="dat__">Idioma: <%= product.idioma %></p>
                    <p class="dat__">Duracion: <%= product.duracion %></p>


                </div>
            </aside>
                

            <!-- FOOTER --->
            <%- include('../partials/footer') %>
        </div>
    </body>

    
    <%- include('../partials/linksJsAbajo') %>
</html>